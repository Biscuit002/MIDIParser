python midi parser:

Takes in .mid file as input and outputs a json structured log file with various attribute data, including time signature, key signature, track names, and times.

To use:
  1. Place .mid file in the 'data' folder.
  2. Run the main.py script in the 'src' directory.
  3. Log outputs located in the 'output' folder
